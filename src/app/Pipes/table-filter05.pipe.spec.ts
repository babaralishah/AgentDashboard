import { TableFilter05Pipe } from './table-filter05.pipe';

describe('TableFilter05Pipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilter05Pipe();
    expect(pipe).toBeTruthy();
  });
});
