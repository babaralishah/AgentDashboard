import { TableFilter03Pipe } from './table-filter03.pipe';

describe('TableFilter03Pipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilter03Pipe();
    expect(pipe).toBeTruthy();
  });
});
