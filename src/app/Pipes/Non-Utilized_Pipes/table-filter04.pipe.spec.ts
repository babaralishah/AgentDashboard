import { TableFilter04Pipe } from './table-filter04.pipe';

describe('TableFilter04Pipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilter04Pipe();
    expect(pipe).toBeTruthy();
  });
});
