import { TableFilter02Pipe } from './table-filter02.pipe';

describe('TableFilter02Pipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilter02Pipe();
    expect(pipe).toBeTruthy();
  });
});
