import { TableFilter06Pipe } from './table-filter06.pipe';

describe('TableFilter06Pipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilter06Pipe();
    expect(pipe).toBeTruthy();
  });
});
